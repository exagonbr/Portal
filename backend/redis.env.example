# Configurações do Redis
# Copie estas configurações para seu arquivo .env

# Configurações básicas do Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Configurações do Redis para filas (opcional - se não definido, usa as configurações principais)
QUEUE_REDIS_HOST=localhost
QUEUE_REDIS_PORT=6379
QUEUE_REDIS_DB=1

# Configurações do Redis para cache estático
REDIS_STATIC_CACHE_DB=2

# Password do Redis (descomente se necessário)
# REDIS_PASSWORD=sua_senha_aqui

# TLS para produção (descomente se necessário)
# REDIS_TLS=true

# Configurações específicas para filas (se diferentes das principais)
# QUEUE_REDIS_PASSWORD=senha_para_filas 